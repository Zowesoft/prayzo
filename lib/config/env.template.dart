class AppEnvTemplate {
  // Copy this file to env.dart and fill in your secrets.
  // IMPORTANT: Use the Supabase REST URL (starts with https://...),
  // NOT the Postgres connection string.
  static const String supabaseUrl = '';
  static const String supabaseAnonKey = '';
  // Optional: if you switch to OneSignal for push
  static const String oneSignalAppId = '';
}
